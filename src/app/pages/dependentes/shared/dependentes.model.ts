import { BaseResourceModel } from 'src/app/shared/models/base-resource.model';

export class Dependentes extends BaseResourceModel {
  construtor(id?: number, nome?:string, cpf?:string, rg?:number, acoes?:any) {}
}
